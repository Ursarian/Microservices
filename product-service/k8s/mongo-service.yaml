apiVersion: v1
kind: Service
metadata:
  name: product-service-db
  namespace: my-services
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: product-service-db
  ports:
    - port: 27017
      targetPort: 27017
      nodePort: 32018
